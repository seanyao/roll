# Dream Log 2026-05-15

## 概要
- 扫描项：死代码 / 架构漂移 / 裁剪候选 / 新兴模式 / 文档覆盖度 / 文档新鲜度
- 发现：15 项标记，6 个 REFACTOR 条目已创建

## 死代码
1. bin/roll 中三个函数从未被生产代码调用：`_write_gitignore()`（L1116）、`_write_env_example()`（L1130）、`detect_project_type()`（L1148）。其中 detect_project_type 有测试但无调用者。
2. `tools/roll-fetch/` 整个目录不可达——SKILL.md 标记 `hidden: true`，且 bin/roll 只链接 `skills/` 不链接 `tools/`。模块随 npm 发布但用户无法通过任何命令调用。
3. `tests/consult-bridge-research/test-cli-reliability.sh` 孤儿研究脚本，未被任何测试运行器引用。
4. `skills/roll-design/ENGINEERING_CHECKLIST.md` 未被 SKILL.md 或其他文件引用。

## 架构漂移
未发现架构漂移。5 个 Bounded Context（Convention Management / Skill Delivery / Autonomous Operation / Observability / Distribution）的代码边界与 docs/domain/context-map.md 一致，跨上下文调用均通过文档化的 ACL/PL 进行。Ubiquitous Language 命名无偏移。

## 裁剪候选
1. **配置读取双实现**：`config_get`（L98，sed 解析，展开 `~`）与 `_config_read_string`（L1996，awk 解析，不展开 `~`）功能重复但行为不一致。分别有 5 和 3 个调用点。
2. **ai_tool_name 死分支**：L57 `"$bn" == "workspace"` 已匹配，L59 elif 中再次包含 `"workspace"` 为不可达代码。
3. **三个写入但从未读取的配置键**：`default_language`、`default_project_type`、`editor` 在 `_install_local` 中写入 config.yaml，但无生产代码读取。
4. **_ensure_tmux 无条件返回 0**：22 行函数声称"确保 tmux"，但所有路径（包括 tmux 未找到且安装失败）均返回 0，不提供任何保障。
5. **roll-fetch 中三项**：`tryLLMNative` 永远返回 `{ success: false }` 是占位空壳；`isBlockedOrLowQuality` 有未使用参数 `source`；`calculateQualityScore` 计算结果无任何代码分支使用。（因 roll-fetch 整体不可达，此三项附属于死代码发现 #2。）

## 新兴模式
1. **Agent 分发 case 块**（6 处）：bin/roll 中 `_agent_run_skill`、`_agent_skill_cmd`、`_peer_call`（2处），scripts/release.sh 中 `_run_changelog_skill`、`_run_release_notes_skill`。每处维护相同的 claude/kimi/deepseek/pi/codex/opencode 映射。新增 agent 需同步 6 处。
2. **gh + commit + slug 前置检查**（7 处）：`_ci_wait`、`_loop_precheck_ci`、`_loop_publish_pr`、`_loop_publish_doc_pr`、`cmd_ci`、`_dash_ci_status`、`_loop_pr_inbox`。每处重复 `command -v gh` + `git rev-parse HEAD` + `_gh_repo_slug`，错误处理各异。
3. **Color + logging 样板**（3 处）：bin/roll、uninstall.sh、tests/consult-bridge-research/ 各自定义 RED/GREEN/YELLOW/CYAN/BOLD/NC + NO_COLOR 守卫 + info/ok/warn/err。

低于 3 次阈值但值得关注：portable MD5 hash（2 处）、YAML frontmatter stripping awk（2 处）、`_loop_publish_pr` / `_loop_publish_doc_pr` 结构 80% 重复。

## 文档覆盖度
**Check A** — 7 个已完成 ≥3 story 的功能区缺少 guide 文档：
- CLI Simplification（3 Done）— 无 guide
- Distribution / npm-distribution（4 Done）— 无 guide
- AI Tools（3 Done）— 无 guide
- QA & Testing / e2e-lifecycle（3 Done）— 无 guide
- Engineering Infrastructure / skill-harness（5 Done）— 无 guide
- Engineering Infrastructure / convention-management（3 Done）— 无 guide
- Changelog / changelog-integration（5 Done）— 无 guide

**Check B** — 无缺口。7 个 EN 文件均有对应 ZH 翻译。
**Check C** — 无散落文件。docs/ 根目录干净。

## 文档新鲜度
- 滞后文档：0 个（超过 30 天未更新但绑定了代码文件）
- 未记录 ENV 变量：0 个（出现 ≥5 次但无文档）
- 架构文档缺失模块：0 个（≥3 个源文件的目录未出现在 docs/domain/）
文档新鲜度良好，无滞后或缺失项。

## 创建的 REFACTOR 条目
- REFACTOR-014: 初始化辅助函数中有三个从未被调用，积累为死代码增加维护噪音
- REFACTOR-015: 一个工具模块随 npm 发布但整条链路不可达，用户无法调用，增加包体积和维护混淆
- REFACTOR-016: 配置值读取存在两套功能重复但行为不一致的实现，改一处容易遗漏另一处导致行为分叉
- REFACTOR-017: agent 命令分发逻辑在 6 处以 case 块形式重复，新增 agent 时需同步全部副本否则运行时失败
- REFACTOR-018: gh/commit/slug 前置检查在 7 个函数中重复且错误处理方式各异，统一行为需逐个排查
- REFACTOR-019: docs: 7 个已完成 ≥3 story 的功能区缺少用户指南，新用户无法从文档体系了解这些功能
