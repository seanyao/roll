# Dream Log 2026-05-18

## 概要
- 扫描项：死代码 / 架构漂移 / 裁剪候选 / 新兴模式 / 文档覆盖度 / 文档新鲜度
- 发现：4 项标记，4 个 REFACTOR 条目已创建

## 死代码

发现 4 个从未在生产路径被调用的函数，全部是昨晚 dream 已经点过名、但被并到其他 REFACTOR 中、最终随父 REFACTOR 一起标 Done 后没人回头清理的残留：

- `_ensure_unreleased()` / `_promote_unreleased()`（约 17 + 4 行）：REFACTOR-021 收口"changelog 双管线"后，两个函数本身已被替代逻辑取代，但函数体和对应单元测试 `tests/unit/release_promote.bats`、`tests/unit/roll_changelog_skill.bats` 全部还在。测试还在跑、还在绿，看上去像活路径，但生产侧没人调用。
- `_loop_self_heal_ci()` / `_loop_clear_heal_state()`（约 16 + 3 行）：REFACTOR-023 把 CI 自愈状态合并到主 state.yaml 后，新路径绕过了这两个函数；但 `skills/roll-loop/SKILL.md` 第 255、264 行还把这两个函数当作 heal 协议的官方接口写在指南里。文档描述了一个代码里不存在的协议。

→ REFACTOR-030

## 架构漂移

未发现跨 Bounded Context 的非授权直接依赖。5 个上下文的 ACL 边界仍清晰，发版后没有新增的越界引用。

## 裁剪候选

未发现可裁剪项。无冻结 feature flag（`ROLL_LOOP_FORCE` / `ROLL_LOOP_NO_HEAL` 两个开关都有真实使用路径），无单一实现的 interface 抽象，无不可达的错误处理分支。

## 新兴模式

发现 macOS/Linux 时间戳兼容写法 `stat -c %Y "$f" 2>/dev/null || stat -f %m "$f" 2>/dev/null || echo 0` 在 bin/roll 中复制了 4 处（约 4648、5011、5031、5324 行），分别在 peer 日志最近时间、dream 日志新鲜度、peer 最后结果、brief 摘要四个 dashboard 子函数里。任何对兼容写法的调整（例如换成读 git mtime 或加 BSD/GNU coreutils 探测）都要四处同步。→ REFACTOR-031

`_dash_*` 14 个子函数共享"存在性 guard + 单条命令 + 单值 echo"的形状，但返回结构各不相同（int、`x|y`、`a b c d e`），变体有意义，不强行抽象。

## 文档覆盖度

**Check A（功能 Guide 覆盖）**：12 个 ≥3 Done stories 的功能区里没有匹配文件名的 `docs/guide/en/<feat>.md`，但项目的 guide 体系是横向主题制（loop / dream / peer / skills / methodology / installation / configuration / conventions / pr-review / testing / faq / overview / changelog / methodology / project-setup / ai-agents），不与 BACKLOG Feature 一对一。沿用 2026-05-17 的判断，这部分缺口由已 Done 的 REFACTOR-019 主轴负责，今天不重复开条目。

**Check B（EN → ZH 翻译）**：`docs/guide/en/` 与 `docs/guide/zh/` 各 15 个文件，diff 完全对齐，无缺口。

**Check C（docs/ 根目录散落文件）**：仅 `docs/features.md`，US-DOC-008 SOT 有意保留，不视为散落。

**Check D（features.md 功能目录覆盖）**：BACKLOG 中有 22 个 Feature 至少 1 个 Done story，`docs/features.md` 当前收录 19 个。漏收 3 个：

- **ai-tools**（US-AI-001/002/003 已 Done）
- **e2e-lifecycle**（US-QA-001/002/003 已 Done，且 `docs/features/e2e-lifecycle.md` 已存在）
- **skill-harness**（US-INFRA-001..005 已 Done）

`scripts/release.sh` Section 8 的 AI 重写应该在每次发版时拉齐目录，今天上午 v2026.518.1/.2/.3 三次发版都跑过，但这三个功能区始终没补进去。说明重写 prompt 的覆盖逻辑或 BACKLOG 摘要传递有遗漏。→ REFACTOR-032

## 文档新鲜度

- 滞后文档：0 个（`docs/features/*.md` 全部在 30 天阈值内有更新）
- 未记录 ENV 变量：0 个（高频 `_LOOP_TAG` / `_LOOP_MUTE_FILE` / `_LOOP_RUNS` / `_LOOP_PROJ_SLUG` / `_LOOP_RUNS_BACKLOG` 都是 bin/roll 内部全局，非用户可配 ENV 入口，按 Check B 的口径不算缺口）
- 架构文档缺失模块：1 个

`lib/` 目录承载 4 个 Python 源文件：`loop-fmt.py`、`model_prices.py`、`roll-loop-status.py`、`roll_render.py`。它们服务 Observability 上下文（loop status 渲染、成本核算、cycle event 流），是近一个月新提取的共享模块（`roll_render.py` 由 b07a9f4 提取，`model_prices.py`、`roll-loop-status.py` 是 US-VIEW-001 / US-LOOP-004 期间引入）。`docs/domain/context-map.md` 把 conventions/ / skills/ / template/ 都标注了对应上下文，但完全没提 `lib/`，读完领域文档的新人无法定位这块代码的归属。→ REFACTOR-033

## 创建的 REFACTOR 条目

- REFACTOR-030：4 个 helper 函数随父 REFACTOR Done 后留存为孤儿，且 2 个仍被 SKILL 文档当作活协议描述
- REFACTOR-031：mtime 兼容写法 4 处复制
- REFACTOR-032：features.md 目录漏收 ai-tools / e2e-lifecycle / skill-harness 三个功能区
- REFACTOR-033：`lib/` Python 共享模块未在 `docs/domain/context-map.md` 出现
