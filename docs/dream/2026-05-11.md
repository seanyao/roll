# Dream Log 2026-05-11

## Summary
- Scans run: Dead Code / Architectural Drift / Pruning / Patterns
- Findings: 5 items flagged, 3 REFACTOR entries created

## Dead Code

Two functions in bin/roll are defined and have test coverage but are never called from any production code path:

- `is_fresh_project()` (bin/roll:996) — checks if a project directory has no source files. No callers in bin/roll or skills/. Has orphaned unit tests in tests/unit/is_fresh_project.bats.
- `_mkscaffold()` (bin/roll:1005) — creates a directory with .gitkeep. No callers, no tests.

Note: `detect_project_type()` (bin/roll:1072) was also found without production callers, but docs/features/cli-simplification-plan.md explicitly marks it for future "refresh_project" use — kept.

→ REFACTOR-003 created.

## Architectural Drift

### Variable name mismatch (silent bug)

`_write_backlog()` (bin/roll:1010,1025) and `_ensure_features_dir()` (bin/roll:1030,1036) append to `_WK_MERGE_SUMMARY`, but the declared array is `_ROLL_MERGE_SUMMARY` (bin/roll:43). The summary printer at bin/roll:931-936 iterates `_ROLL_MERGE_SUMMARY`, so BACKLOG.md and docs/features/ creation/unchanged status is silently dropped from the `roll init` output.

→ REFACTOR-004 created.

### Skills absent from methodology.md

Four skills exist in skills/ but are never mentioned in docs/methodology.md:
- roll-idea — fast backlog capture; logically belongs in Loop A
- roll-notes — development diary; referenced by roll-.changelog but has no loop home
- roll-doctor — toolchain health diagnostics; system utility outside the 3-loop model
- roll-release — version publish for roll itself; meta-operation, not a delivery operation

No REFACTOR entry created (docs-only gap, no complexity or bug risk). Recommend adding a "System Utilities" section to methodology.md in a future pass.

## Pruning Candidates

`cmd_release()` (bin/roll:2223-2225) is a one-liner wrapper over `_agent_run_skill "roll-release"`. It is consistent with all other cmd_* wrappers — inlining would harm symmetry more than it helps. Not flagged.

`_ai_dir()`, `_ai_config()`, `_ai_src()` (bin/roll:171-173) are micro-helpers for field extraction. Readability benefit outweighs the refactoring cost. Not flagged.

Nothing flagged.

## Emerging Patterns

### AI tool iteration — 4× duplication

The pattern of reading `_get_ai_tools`, extracting fields via `_ai_dir/config/src`, and processing entries appears at approximately bin/roll:365-458, bin/roll:497-507, bin/roll:1136-1141, and one additional location. Each instance duplicates the loop scaffolding. A `_for_each_ai_tool callback` helper would consolidate this.

→ REFACTOR-005 created.

### Test setup boilerplate — 13 test files

All bats tests in tests/unit/ share near-identical setup/teardown: source bin/roll, mktemp, rm -rf. A shared tests/unit/helpers.bash would reduce ~60 LOC of boilerplate. Not flagged (low complexity impact, no bug risk).

### Error guard pattern — 50+ occurrences

`[[ condition ]] || { err "..."; return 1; }` appears throughout bin/roll. Extraction into `require_file()` / `require_dir()` helpers would be cleaner, but the current pattern is idiomatic bash and poses no maintenance risk. Not flagged.

## REFACTOR Entries Created
- REFACTOR-003: Delete `is_fresh_project()`, `_mkscaffold()`, and orphaned test file
- REFACTOR-004: Fix `_WK_MERGE_SUMMARY` → `_ROLL_MERGE_SUMMARY` in `_write_backlog()` + `_ensure_features_dir()`
- REFACTOR-005: Extract `_for_each_ai_tool()` to eliminate 4× AI iteration duplication
