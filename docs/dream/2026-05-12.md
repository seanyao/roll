# Dream Log 2026-05-12

## 概要
- 扫描项：死代码 / 架构漂移 / 裁剪候选 / 新兴模式
- 发现：1 项标记，1 个 REFACTOR 条目已创建

## 死代码

`is_fresh_project()`（bin/roll:1042）和 `_mkscaffold()`（bin/roll:1051）在生产代码中仍无调用方，孤儿测试 `tests/unit/is_fresh_project.bats` 亦无变化。REFACTOR-003 待办，未有新发现。

`detect_project_type()`（bin/roll:1118）已在 docs/features/cli-simplification-plan.md 明确标注为保留，供未来 `refresh_project` 使用，不计入死代码。

未发现新的死代码。

## 架构漂移

`_write_backlog()`（bin/roll:1056, 1071）和 `_ensure_features_dir()`（bin/roll:1076, 1082）仍使用未声明变量 `_WK_MERGE_SUMMARY`，而声明和读取处均为 `_ROLL_MERGE_SUMMARY`（bin/roll:43, 977-982）。REFACTOR-004 待办，bug 未修复。

docs/domain/ 目录仍未建立（US-DOC-003 待办），无法执行上下文映射扫描——与昨日结论一致，不新建条目。

未发现新的架构漂移。

## 裁剪候选

新增的 `_ci_wait()`（bin/roll:2696）和 `_loop_enforce_ci()`（bin/roll:2751）是为 FIX-024 准备的基础设施，逻辑完整，调用关系清晰，不属于裁剪对象。

未发现可裁剪项。

## 新兴模式

### 单元测试 setup 样板 — 22 个文件无共享 helper

当前 36 个单元测试文件中，22 个含独立 `setup()` 函数，21 个在 `setup()` 内直接 `source "$ROLL_BIN"`，共计 63 处 `mktemp`/`rm -rf` 行。`tests/integration/helpers.bash` 已将集成测试的此类样板提取为共享工具（4 个文件受益），但单元测试层没有对等文件。

规模已从昨日梦境记录的 ~13 个文件增长至 22 个，不对称愈发明显。新建 `tests/unit/helpers.bash` 将直接消除这 22 个文件中重复的 `source`、`mktemp`、`rm -rf`、`NO_COLOR`/`TERM` 导出逻辑，降低未来测试新增时的认知负担。

→ REFACTOR-007 已创建。

## 创建的 REFACTOR 条目
- REFACTOR-007: 新建 `tests/unit/helpers.bash`，统一 22 个单元测试文件的 setup/teardown 样板（参照 tests/integration/helpers.bash 模式）
