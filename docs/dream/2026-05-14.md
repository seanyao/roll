# Dream Log 2026-05-14

## 概要
- 扫描项：死代码 / 架构漂移 / 裁剪候选 / 新兴模式 / 文档覆盖度 / 文档新鲜度
- 发现：4 项标记，3 个 REFACTOR 条目已创建

## 死代码
`tests/unit/helpers/test-injectable-bb.js` — 0 条外部引用，文件名前缀 `test-` 但位于 helpers/ 目录，疑似遗留测试文件未被测试套件收录。已纳入 REFACTOR-011 一并处理。

其余 JS 文件（smart-web-fetch.js、injectable-bb.js）均有正常引用，无问题。

## 架构漂移
未发现跨 Bounded Context 的非法导入。`docs/domain/context-map.md` 定义的 5 个上下文（Convention Management / Skill Delivery / Autonomous Operation / Observability / Distribution）与代码结构对应良好。

## 裁剪候选
`.claude/worktrees/` 下残留 3 个本地 worktree，对应分支最近提交分别为 2026-05-04、2026-05-09、2026-05-12。远端 `claude/*` 分支已由 session 清理机制删除，但本地 worktree 未被 `git worktree remove` 清理。长期积累会导致 `git worktree list` 越来越嘈杂，详见 REFACTOR-011。

## 新兴模式
`scripts/release.sh` 中 `_detect_agent()` 的 YAML config 读取逻辑与 `bin/roll` 中 `config_get()` 实现的功能高度重叠，但两处独立维护。目前仅 1 处重复（未到 3 次阈值），但 config schema 一旦变更就会静默漂移，已提前标记 REFACTOR-012。

## 文档覆盖度
- **5A（Done story 缺 guide 文档）**：US-AUTO 有 59 条 Done story，`docs/guide/en/` 有 loop.md 和 dream.md 覆盖主要自动化功能，无明显缺口。其余 feature 组（US-SKILL、US-INFRA 等）story 数 ≤11，覆盖尚可接受。
- **5B（EN/ZH 翻译对称）**：`docs/guide/en/` 与 `docs/guide/zh/` 文件列表完全一致，无缺口。
- **5C（docs/ 根目录散落）**：无散落文件，目录结构符合规范。

## 文档新鲜度
- 滞后文档：0 个（feature docs 均在近 30 天内更新）
- 未记录 ENV 变量：2 个（`$ROLL_CONFIG` 18 次引用 / `$ROLL_GLOBAL` 9 次引用，docs/ 中 0 次提及，详见 REFACTOR-013）
- 架构文档缺失模块：0 个

## 创建的 REFACTOR 条目
- REFACTOR-011：本地 worktree 未随 session 清理一并回收
- REFACTOR-012：`_detect_agent()` 与 `config_get()` 逻辑重叠，双维护风险
- REFACTOR-013：`$ROLL_CONFIG` / `$ROLL_GLOBAL` 无文档，用户靠读源码才能发现
