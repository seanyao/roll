# Dream Log 2026-05-17

## 概要
- 扫描项：死代码 / 架构漂移 / 裁剪候选 / 新兴模式 / 文档覆盖度 / 文档新鲜度
- 发现：9 项标记，5 个 REFACTOR 条目已创建

## 死代码

发现 7 个从未被调用的函数：

- `merge_convention()`（约 160 行）：convention 合并逻辑的完整实现，从未被执行路径引用。两个现有合并函数各自用内联逻辑，这段代码处于孤岛状态。→ REFACTOR-025
- `_loop_enforce_tcr()`（约 18 行）：loop 在故事完成后检查 TCR 微提交数量的校验逻辑，US-AUTO-010 标记 Done，但调用点缺失。→ REFACTOR-026
- `_loop_precheck_ci()`（约 14 行）：loop 启动前检查当前 HEAD CI 状态的预检逻辑，从未被接入。→ REFACTOR-027
- `_loop_self_heal_ci()`（约 16 行）：CI 失败时的自愈重试逻辑。注：REFACTOR-023 已跟踪状态计数问题，此为调用缺失问题。
- `_loop_clear_heal_state()`（约 3 行）：清理自愈计数状态，孤立无调用。
- `_promote_unreleased()`（约 4 行）：changelog 版本提升辅助函数，无调用。
- `_ensure_unreleased()`（约 17 行）：changelog Unreleased 节初始化辅助函数，无调用。后两个与 REFACTOR-021（changelog 双管线）相关，纳入该条目范围。

## 架构漂移

未发现跨 Bounded Context 的非授权直接依赖。5 个上下文的 ACL 边界均清晰。

`roll-.changelog` 同时服务 Distribution（发版）和 Observability（变更追踪）两个关注点，上下文归属有歧义，但功能上有意为之，建议在 context-map.md 中明确说明混合角色。不单独开 REFACTOR 条目。

## 裁剪候选

未发现可裁剪项。无冻结 feature flag，无单一实现的 interface 抽象，无不可达的错误处理分支。

`_ai_dir()` / `_ai_config()` / `_ai_src()` 三个单行字段提取函数各被调用 5–9 次，语义明确，保留合理。

## 新兴模式

`cmd_backlog` 展示逻辑中，FIX / US / REFACTOR / IDEA 四种条目类型使用了结构完全一样的 `awk -F'|'` 解析循环（bin/roll 约 4748–4834 行），共计约 4 处重复。格式变更时需同步四处。→ REFACTOR-028

`_loop_*` 子系统已有 25+ 个函数，是单文件中最大的内聚模块，但整体可读性尚可，暂不标记为架构问题。

## 文档覆盖度

**Check A（功能覆盖）**：docs/guide/en/ 现有 8 个指南文件，覆盖 loop / dream / peer / skills / methodology 等横向主题。无新增功能区达到"≥3 Done stories 且无指南"的状态——所有此类缺口已由 REFACTOR-019 追踪。无新条目。

**Check B（EN → ZH 翻译）**：docs/guide/en/ 和 docs/guide/zh/ 均有 8 个文件，一一对应，全部通过。无缺口。

**Check C（docs/ 根目录散落文件）**：发现 `docs/features.md`，该文件为产品功能索引 SOT（US-DOC-008），属于有意保留在根目录，不属于散落文件。无动作。

## 文档新鲜度

- 滞后文档：0 个（所有 feature doc 近期均有更新，最老的约 28–30 天，未超阈值）
- 未记录 ENV 变量：4 个（出现 ≥5 次但文档缺失）
- 架构文档缺失模块：0 个（conventions / skills / template 均出现在 docs/domain/）

发现 ROLL_MERGE_SUMMARY（代码中 19+ 次引用）、ROLL_PKG_CONVENTIONS（11 次）、ROLL_LOOP_FORCE（6 次）、ROLL_TEMPLATES（9 次，docs 仅 1 次提及）在 docs/ 中完全缺失或严重欠文档，用户和贡献者无法从文档中发现这些配置入口。→ REFACTOR-029

## 创建的 REFACTOR 条目

- REFACTOR-025：convention 合并大型死代码分支
- REFACTOR-026：loop TCR 校验逻辑已实现但未接入
- REFACTOR-027：loop 启动前 CI 预检逻辑已实现但未接入
- REFACTOR-028：BACKLOG 展示四类条目的解析循环重复四次
- REFACTOR-029：ROLL_MERGE_SUMMARY 等四个 ENV 变量文档缺失
