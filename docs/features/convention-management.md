# Convention Management

<a id="us-conv-001"></a>
## US-CONV-001 全局约定加入 Goal-Driven Execution 规则 ✅

**Created**: 2026-05-14
**Completed**: 2026-05-14

- As a developer using Roll agents
- I want agents to state verifiable success criteria before implementing
- So that "make it work" style tasks get clarified rather than blindly executed

**Domain Model:**
- Context: Convention Management
- Aggregate: AgentConvention (Root) owns [WorkflowRule]
- Events raised: [ConventionUpdated] → Skill Delivery Context

**AC:**
- [x] `conventions/global/AGENTS.md` Section 4 (Workflow) 新增 Goal First 规则（≤5 行）
- [x] 规则格式：先定义可验证目标，多步任务列出每步的 verify checkpoint，弱标准要求人工确认
- [x] 修改后 `conventions/global/AGENTS.md` 总行数 ≤100 行（89 行）
- [x] Roll 项目根目录的 `AGENTS.md` 同步更新

**Files:**
- `conventions/global/AGENTS.md`
- `AGENTS.md`

---

<a id="us-conv-002"></a>
## US-CONV-002 AGENTS.md 加入 "Where to Look" 导航段，roll-design 维护指针 ✅

**Created**: 2026-05-14
**Completed**: 2026-05-14

- As a developer onboarding any agent into a Roll-managed project
- I want AGENTS.md to have a structured "Where to Look" section pointing to docs/domain/ and docs/features/
- So that any agent can navigate project knowledge without reading all the code first

**Domain Model:**
- Context: Convention Management
- Aggregate: AgentConvention (Root) owns [NavigationPointer]
- Events raised: [PointerRegistered] → Skill Delivery Context
- Cross-context: roll-design (Skill Delivery) writes back to Convention Management artifact

**AC:**
- [x] `conventions/global/AGENTS.md` 新增 `## 8. Where to Look` section（含 docs/domain/ 和 docs/features/ 指针）
- [x] 所有 `conventions/templates/*/AGENTS.md` 同步加入 `## 5. Where to Look` section
- [x] roll-design Domain Slice 完成后：检查 AGENTS.md 是否已有 `docs/domain/` 指针，若无则 append 一行，若有则不操作（幂等）
- [x] roll-design Clarify 阶段：触发前先内部走三步定位（产品端 / 角色 / 业务域），已定位的作为上下文前缀呈现，不作为问题重复询问
- [x] 修改后 `conventions/global/AGENTS.md` 总行数 ≤100 行（95 行）

**Files:**
- `conventions/global/AGENTS.md`
- `conventions/templates/frontend-only/AGENTS.md`
- `conventions/templates/backend-service/AGENTS.md`
- `conventions/templates/cli/AGENTS.md`
- `conventions/templates/fullstack/AGENTS.md`
- `skills/roll-design/SKILL.md`

---

<a id="us-conv-003"></a>
## US-CONV-003 roll-doc 为存量项目生成 AGENTS.md 导航结构 ✅

**Created**: 2026-05-14
**Completed**: 2026-05-14

- As a developer adopting Roll on a legacy project
- I want roll-doc to scan the project and generate a "Where to Look" section in AGENTS.md
- So that agents entering the project have navigable context from day one

**Domain Model:**
- Context: Convention Management
- Aggregate: AgentConvention (Root) owns [NavigationPointer]
- Events raised: [AgentsMdBootstrapped] → Observability Context

**AC:**
- [x] roll-doc Phase 3 (Fill) 新增目标：检测项目是否缺 AGENTS.md 或缺 `## Where to Look` 段
- [x] 扫描现有 docs/ 结构，仅生成指向**实际存在**目录的指针（不生成指向不存在目录的指针）
- [x] 若 `docs/domain/context-map.md` 存在，读取 Bounded Context 名称，在指针行后追加一行摘要
- [x] 生成内容以 `> Draft — auto-generated by roll-doc on YYYY-MM-DD` 标注
- [x] 幂等：重复运行不重复写入已有指针（already present 检查），不覆盖 `## Where to Look` 段之外的任何人工内容

**Files:**
- `skills/roll-doc/SKILL.md`

**Dependencies:**
- Depends on: US-CONV-002（依赖 Section 5 格式标准定义）
