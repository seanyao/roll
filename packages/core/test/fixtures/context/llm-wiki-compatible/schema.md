# Fixture schema

Ordinary pages use `roll.context-page/v1`; page types are repository-defined strings.
Roll's required safety metadata coexists with nashsu editor fields such as `type`, `tags`, `related`, `created`, and `updated`.
