Generating release notes via claude...
The release script's prompt cut off without pasting the bullets. Based on commits since v2026.515.1, here's the draft Release Notes:

## 稳定性

- Loop 现在能扛住系统休眠 — 笔记本盖上盖子或长时间挂机不会再把 cycle 拽死 `[loop]`
- 卡死的 inner 脚本会被发现并清理 — 心跳超过 30 分钟自动判定僵死，下一轮 cycle 正常启动 `[loop]`
- 跑完没产出的 worktree 立刻清掉，本地不再积压空壳 `[loop]`
- 发版脚本从临时 worktree 也能正常发布，不再因路径解析失败而中断 `[loop]`
- 项目识别更稳 — 在 worktree 里执行 loop 命令不会再被当成另一个项目 `[loop]`

## 约定与导航

- `ROLL_HEARTBEAT_TIMEOUT` 环境变量写入文档 — 想调心跳阈值有据可查
