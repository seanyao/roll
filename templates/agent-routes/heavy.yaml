# Agent routing config — heavy preset, schema v3 (US-AGENT-028)
#
# 适合多 agent 协作的复杂项目：pi 跑短小 easy 故事,claude 啃 hard 大故事 /
# 高风险架构,default 走 claude 兜底。fallback 用 pi 作为机械顶替。

schema: v3

easy:     { agent: pi }
default:  { agent: claude }
hard:     { agent: claude }
fallback: { agent: pi }
