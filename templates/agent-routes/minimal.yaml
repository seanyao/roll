# Agent routing config — minimal preset, schema v3 (US-AGENT-028)
#
# 适合本机只有一个可用 agent 的项目：三档(easy/default/hard)全锁同一个 agent,
# 等价于 `roll agent use pi`。fallback 也指向同一个,因为没有别的可顶。

schema: v3

easy:     { agent: pi }
default:  { agent: pi }
hard:     { agent: pi }
fallback: { agent: pi }
