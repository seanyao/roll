# Cross-Agent CLI Reliability Report

- **Date**: 2026-05-08T23:21:39+08:00
- **Host**: Darwin Sean-MacBook-Air.local 25.3.0 Darwin Kernel Version 25.3.0: Wed Jan 28 20:53:31 PST 2026; root:xnu-12377.91.3~2/RELEASE_ARM64_T8103 arm64

## Tool Versions

```
Kimi CLI: kimi, version 1.40.0
Claude Code: 2.1.133 (Claude Code)
```

## Group A: Basic Non-Interactive Invocation

[0;36m[test][0m Running: A1: Kimi --quiet simple prompt
[0;36m[test][0m Command: kimi --quiet -p 'Explain what a bash shebang is, in one sentence.'

## A1: Kimi --quiet simple prompt

- **Command**: `kimi --quiet -p 'Explain what a bash shebang is, in one sentence.'`
- **Exit Code**: 0
- **Elapsed**: 7.33s
- **Stdout Length**:      168 bytes
- **Stderr Length**:       70 bytes

### stdout
```
A bash shebang (`#!/bin/bash`) is a special line at the start of a script that tells the operating system which interpreter program should execute the file's contents.
```

### stderr
```

To resume this session: kimi -r fce25ed5-4cd2-4319-b4f1-282495207241
```

[0;32m[test][0m A1: Kimi --quiet simple prompt: SUCCESS (7.33s,      168 bytes)
[0;36m[test][0m Running: A2: Claude -p simple prompt
[0;36m[test][0m Command: claude -p --output-format text 'Explain what a bash shebang is, in one sentence.'

## A2: Claude -p simple prompt

- **Command**: `claude -p --output-format text 'Explain what a bash shebang is, in one sentence.'`
- **Exit Code**: 0
- **Elapsed**: 5.71s
- **Stdout Length**:      143 bytes
- **Stderr Length**:        0 bytes

### stdout
```
A bash shebang (`#!/bin/bash`) is the first line of a script that tells the operating system which interpreter to use when executing the file.
```

### stderr
```
```

[0;32m[test][0m A2: Claude -p simple prompt: SUCCESS (5.71s,      143 bytes)
## Group B: Code Analysis (No Tools Needed)

[0;36m[test][0m Running: B1: Kimi --quiet code review
[0;36m[test][0m Command: kimi --quiet -p 'Review this bash function for safety issues. List exactly 2 problems if any:
```bash
#!/usr/bin/env bash
set -euo pipefail

cleanup() {
  rm -rf /tmp/workdir
}

trap cleanup EXIT
```'

## B1: Kimi --quiet code review

- **Command**: `kimi --quiet -p 'Review this bash function for safety issues. List exactly 2 problems if any:
```bash
#!/usr/bin/env bash
set -euo pipefail

cleanup() {
  rm -rf /tmp/workdir
}

trap cleanup EXIT
```'`
- **Exit Code**: 0
- **Elapsed**: 38.06s
- **Stdout Length**:      618 bytes
- **Stderr Length**:       70 bytes

### stdout
```
Two safety issues:

1. **Predictable path enables attacks and races**: Hardcoding `/tmp/workdir` instead of using `mktemp -d` allows an attacker to pre-create that path (as a directory or symlink) before the script runs. Concurrent instances of this script also race against the same path, potentially deleting each other’s data.

2. **Unconditional deletion without verification**: The `EXIT` trap fires even if this script never created `/tmp/workdir`. It removes the path without checking ownership, contents, or whether the directory was created by the current invocation, risking destruction of unrelated data.
```

### stderr
```

To resume this session: kimi -r b31b1a32-af45-41e5-901e-694cd6414a10
```

[0;32m[test][0m B1: Kimi --quiet code review: SUCCESS (38.06s,      618 bytes)
[0;36m[test][0m Running: B2: Claude -p code review
[0;36m[test][0m Command: claude -p --output-format text 'Review this bash function for safety issues. List exactly 2 problems if any:
```bash
#!/usr/bin/env bash
set -euo pipefail

cleanup() {
  rm -rf /tmp/workdir
}

trap cleanup EXIT
```'
